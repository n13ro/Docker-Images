# Docker-Images
Образы для работы с разными технологиями и интеграциями

Чтобы запустить образ введите команду

```code
docker compose -f dockercompese.{name_image}.yml up -d
```
## Обязательно
```-f``` — указывает файл с кастомным названием

## Опционально
```-d``` — фоновый режим
